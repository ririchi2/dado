{
  "name": "dado",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "stylelint": "npx stylelint src/css/index.css",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "stylelint": "^13.13.1",
    "stylelint-config-standard": "^22.0.0",
    "vite": "^2.6.0"
  }
}
